/*
package com.example.demo_maciek.api.person

import com.example.demo_maciek.model.Person
import org.springframework.data.domain.Page
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Service

@Service
class PersonService(private val personRepository: PersonRepository) {
    fun returnAll(): Page<Person> {
        return personRepository.findAll(Pageable.unpaged())
    }

    */
/*fun postPerson(name: String, age: Int): Person {
        return personRepository.save(Person(name, age))
    }*//*

}*/
